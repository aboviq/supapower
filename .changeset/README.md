# Changesets

This folder holds the [changesets](https://github.com/changesets/changesets) for the repo.

Run `bun run changeset` to describe a change, commit the generated markdown file with your pull
request, and the release workflow turns it into a version bump and a changelog entry.
