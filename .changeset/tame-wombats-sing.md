---
'@supapower/react': minor
---

First release: re-exports everything from `@electric-sql/pglite-react`, a ready-made `extensions` object covering both the `live` and `supapower` PGlite extensions, and a `useSupapowerStatus` hook that stays up to date with Supapower's sync status.
