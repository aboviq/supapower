---
'@supapower/vue': minor
---

First release: re-exports everything from `@electric-sql/pglite-vue`, a ready-made `extensions` object covering both the `live` and `supapower` PGlite extensions, typed `providePGlite`/`injectPGlite`, and a `useSupapowerStatus` composable that stays up to date with Supapower's sync status.
