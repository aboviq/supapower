/** Channel the change trigger notifies on whenever it queues something. */
export const CHANGES_CHANNEL = 'supapower_changes';

/** Channel `redownload()` notifies on to wake whichever tab is downloading. */
export const REFRESH_CHANNEL = 'supapower_refresh';
